///////////////////////////////////////////
// performs a cubmap lookup and outputs it
///////////////////////////////////////////

//GLES 2.0 / WebGL

#version 100

precision mediump float;

varying vec3 vPosition;

uninform samplerCube uSkybox;

void main()
{
	gl_FragColor = textureCube(uSkybox, vPosition);
}